# 📱 Настройка PWA (Progressive Web App)

Приложение теперь поддерживает установку как PWA! Пользователи могут установить его на рабочий стол или домашний экран устройства.

## ✅ Что уже сделано

1. ✅ Установлен плагин `vite-plugin-pwa`
2. ✅ Настроен `vite.config.ts` с конфигурацией PWA
3. ✅ Обновлен `index.html` с мета-тегами
4. ✅ Создан Service Worker для офлайн работы и кеширования
5. ✅ Настроено кеширование шрифтов и Firebase запросов

## 🎨 Создание иконок

### Способ 1: Автоматическая генерация (РЕКОМЕНДУЕТСЯ)

1. Откройте файл `public/generate-icons.html` в браузере
2. Нажмите кнопки "Скачать" для обоих размеров
3. Сохраните файлы `icon-192.png` и `icon-512.png` в папку `public/`

### Способ 2: Онлайн генератор

1. Перейдите на [Favicon Generator](https://favicon.io/favicon-generator/)
2. Создайте простую иконку с текстом "Г22"
3. Или используйте [RealFaviconGenerator](https://realfavicongenerator.net/)
4. Скачайте иконки размеров 192x192 и 512x512
5. Поместите их в папку `public/` как `icon-192.png` и `icon-512.png`

### Способ 3: Использовать SVG

1. Откройте `public/icon.svg` в графическом редакторе
2. Экспортируйте как PNG в размерах 192x192 и 512x512
3. Сохраните в папку `public/`

## 🚀 Как использовать

### 1. Соберите проект

```bash
npm run build
```

### 2. Протестируйте локально

```bash
npm run preview
```

### 3. Откройте в браузере

- Chrome/Edge: Откройте DevTools (F12) → вкладка "Application" → "Manifest"
- Проверьте, что манифест загружается без ошибок

### 4. Установка приложения

**На компьютере:**
- Chrome/Edge: иконка установки появится в адресной строке справа
- Или через меню: "Установить приложение"

**На Android:**
- Chrome: появится баннер "Добавить на главный экран"
- Или через меню: "Добавить на главный экран"

**На iOS:**
- Safari: нажмите "Поделиться" → "На экран Домой"

## 🔧 Возможности PWA

✅ **Установка на устройство** - работает как нативное приложение  
✅ **Офлайн режим** - базовые функции работают без интернета  
✅ **Кеширование** - быстрая загрузка повторных посещений  
✅ **Уведомления** - поддержка push-уведомлений (можно добавить позже)  
✅ **Краткие пути** - быстрый доступ к расписанию и заменам из меню приложения  

## 📝 Дополнительные настройки

### Изменить цвет темы

В `vite.config.ts` найдите `theme_color: '#4f46e5'` и измените на нужный цвет.

### Добавить больше ярлыков

В `vite.config.ts` в секции `manifest.shortcuts` добавьте новые элементы.

### Настроить офлайн страницу

В `workbox.runtimeCaching` можно добавить специальную страницу для офлайн режима.

## ⚠️ Важно

- Иконки должны быть в папке `public/` **перед** сборкой проекта
- После изменений в `vite.config.ts` нужно пересобрать проект
- Service Worker автоматически обновляется при новых версиях

## 🐛 Решение проблем

**Иконки не отображаются:**
- Проверьте, что файлы `icon-192.png` и `icon-512.png` находятся в папке `public/`
- Пересоберите проект: `npm run build`

**Приложение не устанавливается:**
- Убедитесь, что сайт работает по HTTPS (обязательно для PWA)
- Проверьте консоль браузера на ошибки

**Service Worker не работает:**
- Откройте DevTools → Application → Service Workers
- Проверьте статус и ошибки

## 📚 Полезные ссылки

- [PWA Builder](https://www.pwabuilder.com/) - проверка PWA
- [Lighthouse](https://developers.google.com/web/tools/lighthouse) - аудит PWA
- [Workbox](https://developers.google.com/web/tools/workbox) - документация по кешированию









